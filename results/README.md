# Results

This directory stores trained model checkpoints (`.pt` files) and evaluation results.

Files generated during training:
- `ais_classification_<model>_lr_<lr>.pt` — Trained model checkpoints
- `ais_classification_model_results.json` — Summary of all model results

These files are excluded from version control via `.gitignore`.
